"""
Tests for {{ agent_name }} tools.
"""

import pytest
{% if uses_kubernetes %}
from unittest.mock import MagicMock, patch
{% endif %}

from {{ package_name }}.tools import (
    # Import your tools here
{% if uses_kubernetes %}
    # Example: get_pods, get_deployments
{% endif %}
)


class TestTools:
    """Tests for agent tools."""

{% if uses_kubernetes %}
    @pytest.fixture
    def mock_k8s_client(self):
        """Create a mock Kubernetes client."""
        with patch("{{ package_name }}.tools.client") as mock_client:
            yield mock_client

    def test_example_tool(self, mock_k8s_client) -> None:
        """Test example tool with mocked K8s client."""
        # Setup mock response
        mock_k8s_client.CoreV1Api.return_value.list_namespaced_pod.return_value.items = []

        # Call your tool
        # result = your_tool()

        # Assert expected behavior
        # assert result == expected_value
        pass
{% else %}
    def test_example_tool(self) -> None:
        """Test example tool."""
        # Call your tool
        # result = your_tool()

        # Assert expected behavior
        # assert result == expected_value
        pass
{% endif %}
