[project]
name = "skill-dev-tools"
version = "0.3.0"
description = "Skill development and testing tools for Kubani"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "structlog>=24.0",
    "httpx>=0.27",
    "python-frontmatter>=1.0",
    "pyyaml>=6.0",
    "duckdb>=0.10.0",
]

[project.optional-dependencies]
temporal = ["temporalio>=1.7"]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/skill_dev_tools"]
