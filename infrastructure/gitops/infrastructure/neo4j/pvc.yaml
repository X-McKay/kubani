apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neo4j-data
  namespace: database
  labels:
    app.kubernetes.io/name: neo4j
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: nas-neo4j
  resources:
    requests:
      storage: 10Gi
