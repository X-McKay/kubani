apiVersion: v1
kind: Secret
metadata:
  name: qdrant-credentials
  namespace: database
  labels:
    app.kubernetes.io/name: qdrant
type: Opaque
stringData:
  # IMPORTANT: This file must be encrypted with SOPS before committing.
  # Run: sops --encrypt infrastructure/gitops/infrastructure/qdrant/secret.enc.yaml > /tmp/enc.yaml && mv /tmp/enc.yaml infrastructure/gitops/infrastructure/qdrant/secret.enc.yaml
  api-key: "REPLACE_WITH_ACTUAL_API_KEY"
