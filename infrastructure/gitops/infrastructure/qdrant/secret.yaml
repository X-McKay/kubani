# Qdrant credentials secret
# This is a placeholder - actual secret should be created via SOPS
apiVersion: v1
kind: Secret
metadata:
  name: qdrant-credentials
  namespace: database
  labels:
    app.kubernetes.io/name: qdrant
type: Opaque
stringData:
  # API key for Qdrant authentication - CHANGE THIS via SOPS
  api-key: "changeme-qdrant-api-key"
