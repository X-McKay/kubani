apiVersion: v1
kind: PersistentVolume
metadata:
  name: nas-neo4j
  labels:
    app.kubernetes.io/name: neo4j
    app.kubernetes.io/component: graph-database
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  csi:
    driver: smb.csi.k8s.io
    volumeHandle: nas-neo4j-unique-id
    volumeAttributes:
      source: "//100.72.32.10/kubani/k8s-volumes/neo4j"
    nodeStageSecretRef:
      name: nas-smb-creds
      namespace: nas-storage
  mountOptions:
    - dir_mode=0777
    - file_mode=0777
    - vers=3.0
    - noperm
    - nobrl
