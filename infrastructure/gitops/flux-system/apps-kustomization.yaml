---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infrastructure/gitops/apps
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  # Enable SOPS decryption for encrypted secrets
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  # Wait for databases to be ready before deploying applications
  dependsOn:
    - name: databases
  # Health checks to ensure applications are ready
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: authentik
      namespace: auth
    - apiVersion: batch/v1
      kind: Job
      name: authentik-live-upgrade-ladder-v1
      namespace: auth
