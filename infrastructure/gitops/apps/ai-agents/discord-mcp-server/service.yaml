---
apiVersion: v1
kind: Service
metadata:
  name: discord-mcp-server
  namespace: ai-agents
  labels:
    app.kubernetes.io/name: discord-mcp-server
    app.kubernetes.io/component: mcp-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: discord-mcp-server
