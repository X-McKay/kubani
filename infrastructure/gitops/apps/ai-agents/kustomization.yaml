---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Agents
  - k8s-monitor/
  - news-monitor/
  - backup-agent/
  - learning-agent/
  - cluster-swarm/
  # MCP Servers
  - discord-mcp-server/
  - kubernetes-mcp-server/
  - kubernetes-mcp-executor/
  - temporal-mcp-server/
  - qdrant-mcp-server/
  - memory-mcp-server/
  # Note: Embeddings are in vllm namespace (vllm-embeddings deployment)
