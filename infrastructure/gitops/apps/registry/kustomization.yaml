apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-agents

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml
  - secret.enc.yaml
  # database-init-job.yaml excluded - runs in database namespace
  # Apply manually: kubectl apply -f database-init-job.yaml
