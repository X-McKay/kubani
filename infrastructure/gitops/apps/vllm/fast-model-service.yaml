apiVersion: v1
kind: Service
metadata:
  name: fast-model-api
  namespace: vllm
  labels:
    app: vllm-fast
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vllm-fast
