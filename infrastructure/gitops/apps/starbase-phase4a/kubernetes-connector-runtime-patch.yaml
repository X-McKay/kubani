apiVersion: apps/v1
kind: Deployment
metadata:
  name: starbase-kubernetes-connector
  namespace: starbase-connectors
spec:
  replicas: 0
  template:
    metadata:
      annotations:
        starbase.io/activation-state: blocked
        starbase.io/blocker: identity-network-and-capacity-gates
