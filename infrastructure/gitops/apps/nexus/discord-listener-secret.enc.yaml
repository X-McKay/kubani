apiVersion: v1
kind: Secret
metadata:
  name: discord-listener-secrets
  namespace: nexus
type: Opaque
stringData:
  # IMPORTANT: This file must be encrypted with SOPS before committing.
  # Run: sops --encrypt infrastructure/gitops/apps/nexus/discord-listener-secret.enc.yaml > /tmp/enc.yaml && mv /tmp/enc.yaml infrastructure/gitops/apps/nexus/discord-listener-secret.enc.yaml
  DISCORD_BOT_TOKEN: "REPLACE_WITH_ACTUAL_BOT_TOKEN"
