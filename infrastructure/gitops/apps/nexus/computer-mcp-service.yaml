apiVersion: v1
kind: Service
metadata:
  name: computer-mcp-server
  namespace: nexus
spec:
  type: ClusterIP
  ports:
    - name: mcp
      port: 8086
      targetPort: mcp
    - name: novnc
      port: 6080
      targetPort: novnc
  selector:
    app.kubernetes.io/name: computer-mcp-server
