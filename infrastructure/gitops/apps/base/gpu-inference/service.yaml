apiVersion: v1
kind: Service
metadata:
  name: gpu-inference
  namespace: default
  labels:
    app: gpu-inference
spec:
  type: ClusterIP
  selector:
    app: gpu-inference
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
