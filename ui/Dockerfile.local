FROM node:22-alpine

WORKDIR /app

# Copy pre-built files
COPY dist ./dist

# Install only the express dependency needed for the server
RUN npm init -y && npm install express --legacy-peer-deps

ENV NODE_ENV=production
ENV PORT=3000

EXPOSE 3000

CMD ["node", "dist/index.js"]
