---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - k8s-monitor/
  - kubernetes-mcp-server/
  # Embeddings moved to vllm namespace (vllm-embeddings deployment)
