apiVersion: v1
kind: Service
metadata:
  name: metadata-registry
  namespace: ai-agents
  labels:
    app.kubernetes.io/name: metadata-registry
    app.kubernetes.io/component: registry-service
    app.kubernetes.io/part-of: kubani
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: metadata-registry
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
